<div class={{cardClasses}}>
  {{chat-channel-title channel=channel}}
  {{#if hasDescription}}
    <p class="chat-channel-preview-card__description">{{channel.description}}</p>
  {{/if}}
  {{#if showJoinButton}}
    <ChatToggleMembershipBtn
      @channel={{this.channel}}
      @afterToggle={{action "afterMembershipToggle"}}
      @joinClasses="btn-primary"
    />
  {{/if}}
  <LinkTo @route="chat.browse" class="chat-channel-preview-card__browse-all">
    {{i18n "chat.browse_all_channels"}}
  </LinkTo>
</div>
